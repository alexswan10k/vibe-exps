1. **Update `index.html` to include `drone-sim`**:
   - Add a new `<a href="drone-sim/index.html" class="project-card" data-category="games">` card for the Drone Sim.
   - Update `projectComplexities` object to include `"drone-sim": <lines of code>`.
2. **Pre-commit tasks**:
   - Verify `drone-sim/index.html` is well-formed.
   - Take screenshots or verify UI visually via script if necessary (following `pre_commit_instructions`).
3. **Submit**:
   - Branch: `add-drone-sim`
   - Commit: "Add Drone Sim 3D prototype"
