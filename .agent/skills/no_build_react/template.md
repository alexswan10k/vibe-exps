# No-Build React Implementation Template

## Component: [App Name]

### 1. HTML Setup
- **React/ReactDOM Builds**: [Link to CDN or local paths.]
- **Script Order**: [List scripts in execution order.]

### 2. Component Architecture
- **Hooks Destructuring**: `const { ... } = React;`
- **Component Pattern**: `const [Name] = (...) => { return React.createElement(...); }`

### 3. Implementation Checklist
- [ ] No `import` or `export` statements used.
- [ ] No `<script type="module">` used.
- [ ] React and ReactDOM loaded via UMD builds.
- [ ] UI rendered via `ReactDOM.createRoot`.
